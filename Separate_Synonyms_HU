gsyn_list = [] # this will hold genes and synonyms that were moxed together in column #4
genelist = [] # this will hold the gene names in the first column of the file.
syn_list = [] # this will hold the separated synonyms 
with open('Full_HU_table2') as full, open('synonyms_HU', 'w') as out:
    for line in full:
        line=line.rstrip()
        line=line.split('\t')
        gene_name = line[0]
        genelist.append(gene_name)
        uniprot = line[1]
        name = line[2]
        gsyn = line[3]
        gsyn_list.append(gsyn)
    for element in gsyn_list:
        syn_list = []
        if ',' in element:
            gslist = element.split(',')#55965
            for syn in gslist:
                if not syn in genelist:
                    syn_list.append(syn)
                    ','.join(syn_list)
        print(syn_list, file = out)

#cat synonyms_HU | tr -d "[]'" > clean_synonym_HU
#cat Full_HU_table2 | cut -f1 > HU_genename
#cat Full_HU_table2 | cut -f2 > HU_protID
#cat Full_HU_table2 | cut -f2 > HU_protname
#cat Full_HU_table2 | cut -f5 > HU_Luge
#paste -d '\t' HU_genename HU_protname clean_synonym_HU HU_Luge > Full_HU_table
#echo GeneName UniprotID GeneSynonym ProteinName LUGEID | tr ' ' '\t' > header
#cat header Full_HU_table > Full_HU_table_New
